/*
** EPITECH PROJECT, 2021
** B-CPE-210-STG-2-1-stumper5-tom.nomine
** File description:
** base
*/

#include "my.h"

static int count_first_str(stumper_t *stp, char **av)
{
    stp->arg1 = malloc(sizeof(char) * my_strlen(av[1) + 1);

    for (int d = 0; av[i][d] != '\0'; d++) {
        if (my_strcmp());
    }
}

int main(int ac, char **av)
{
    stumper_t *stp = malloc(sizeof(stumer_t));

    if (stp == NULL)
        return (84);
    free(stp);
    return (0);
}
